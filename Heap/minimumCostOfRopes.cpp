#include <bits/stdc++.h>

using namespace std;

// Minimum Cost of ropes
//using minHeap
long long minCost(long long arr[], long long n)
{
    
}

int main()
{
    int n;
    cin >> n;
    long long arr[n];
    for (long long &i : arr)
        cin >> i;
}